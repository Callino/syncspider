<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Inherit Form View to Modify it -->
        <record id="view_move_form" model="ir.ui.view">
            <field name="name">view_move_form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">

                <xpath expr="//group[@id='header_right_group']" position="inside">
                    <!--<field name="delivery_weight"/>-->
                    <label for="delivery_tariff"/>
                    <div>
                        <field name="delivery_tariff"/>
                    </div>
                    <label for="move_package_ids"/>
                    <div>
                        <field name="move_package_ids" nolabel="1">
                            <tree editable="bottom">
                                <field name="name"/>
                                <field name="weight"/>
                                <field name="move_id" invisible="1"/>
                            </tree>
                        </field>
                        <button name="send_delivery_webhook" type="object" string="Label erstellen"/>
                    </div>
                </xpath>

            </field>
        </record>

    </data>
</odoo>